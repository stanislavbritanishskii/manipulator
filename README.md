# manipulator
RoboArm is a low-budget 3D-printed manipulator built from off-the-shelf parts, random stepper motors and an ESP32. The ESP32 handles low-level motion and exposes a simple API over USB/Bluetooth/WiFi so you can control the arm from any higher-level system.
